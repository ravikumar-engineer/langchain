# langchain
LangChain is an open-source framework for building applications powered by large language models (LLMs). It provides tools to connect language models with external data, create reusable prompt templates, build modular pipelines (chains), add memory, integrate with vector stores and retrievers
